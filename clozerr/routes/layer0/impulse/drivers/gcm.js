
var registry = global.registry;

var driver_push_message = function( data, users ){
	// Send a push message to the given users.
}

var driver_broadcast_message = function( data ){
	// Broadcast message to everyone at once.
}
