
var handle_user_login_facebook = function( params ){
    var id = params.social_id;
    // Put facebook check url.
    // Login the user.
    
}

// This function handles standard user login. No questions asked.
var handle_user_login = function( params, user ){
    // Create a token for the user.
    // return the token.

}
